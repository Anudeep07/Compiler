package compiler;

public enum TokenType {
    LEFT_PARAN, RIGHT_PARAN, SEMICOLON, COMMA, COLON, PLUS, MINUS,

    IDENTIFIER, LITERAL,

    INT, MAIN, CHAR, SWITCH, BEGIN, CASE, PRINTF, BREAK,
    END, RETURN,

    EOF
}